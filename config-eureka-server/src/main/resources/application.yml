server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/ ##Eureka Server???

#eureka.client.register-with-eureka??????????Eureka Server???????Eureka Server?????false?
#eureka.client.fetch-registry?????Eureka Server?????????????Eureka Server?????false?
#eureka.client.service-url.default-zone??Eureka Server??????Eureka Client??Eureka Server?????????????